{
    "name": "get_postal_codes",
    "description": "Get the information about a single postal code",
    "input_schema": {
        "type": "object",
        "properties": {
            "postal_code": {
                "type": "string",
                "description": "Five-digit US postal code"
            }
        }
    },
    "required": ["postal_code"]
}