{
    "name": "get_postal_code",
    "description": "Get the information about a single postal code",
    "input_schema": {
        "type": "object",
        "properties": {
            "postal_code": {
                "type": "string",
                "description": "Five-digit US postal code, e.g. 29910"
            }
        }
    },
    "required": ["postal_code"]
}